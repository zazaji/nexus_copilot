// src-tauri/src/services/agent.rs
// This file has been removed.
// The logic for starting, stopping, and polling agent tasks is now handled directly
// by the frontend calling the respective commands in `src/commands/agent.rs`.
// This centralizes the logic and makes the frontend the single source of truth for task state management.